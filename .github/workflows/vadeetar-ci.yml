name: vadeetar CI/CD Pipeline

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Setup Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.10'
          
      - name: Run project tests
        run: |
          echo "🚀 Запуск проекта vadeetar..."
          python src/main.py
          echo "✅ Все тесты пройдены!"
